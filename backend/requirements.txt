fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# LangChain – agent, MCP adapter, and LLM providers
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-mcp-adapters>=0.2.0

# Market data (used by the /market REST endpoints)
yfinance>=0.2.0

# PostgreSQL async driver (Supabase direct connection)
asyncpg>=0.29.0

# Async HTTP client (Alpaca API calls)
httpx>=0.27.0
