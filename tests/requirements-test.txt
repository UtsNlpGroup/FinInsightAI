# ── Core test framework ────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
pytest-timeout>=2.3.0

# ── Async HTTP mocking (for MCP / Alpaca calls) ───────────────────────────────
respx>=0.21.0
httpx>=0.27.0

# ── FastAPI test client ────────────────────────────────────────────────────────
# FastAPI and its deps are already in backend/requirements.txt
# but we need them accessible in the test environment
fastapi>=0.115.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
uvicorn[standard]>=0.30.0

# ── LangChain (needed for AgentService imports) ───────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-mcp-adapters>=0.2.0

# ── Market data ────────────────────────────────────────────────────────────────
yfinance>=0.2.0
asyncpg>=0.29.0

# ── RAG evaluation ────────────────────────────────────────────────────────────
ragas>=0.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
langchain-community>=0.3.0

# ── Performance / benchmarking ────────────────────────────────────────────────
pytest-benchmark>=4.0.0

# ── Other ingestion deps ──────────────────────────────────────────────────────
langchain-text-splitters>=0.3.0
transformers>=4.40.0
torch>=2.3.0
